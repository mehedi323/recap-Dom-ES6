function add(num1, num2=0){
    const result = num1 + num2;
    console.log(result);
    return result;
}
const sum = add(20)


const name = `amr sonar
bangla ami tmake onk valobasi`;
console.log(name);

const number = [30, 20, 12, 10];
const student = {name: 'Rohim Ali', age:26};
const result = `sum of ${number[0]} is ${student.age} or ${number[0] + student.age}`;
console.log(result);